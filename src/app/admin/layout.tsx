// This layout is intentionally minimal. Each admin sub-page manages its own layout.
export default function AdminRootLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
